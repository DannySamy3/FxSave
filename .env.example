# Environment Configuration (Copy to .env.local to override)
# These are optional - defaults work fine for local development

# Next.js
NODE_ENV=development
NEXT_PUBLIC_APP_NAME=Gold Price Prediction

# API
NEXT_PUBLIC_API_TIMEOUT=30000

# Data paths (relative to project root)
NEXT_PUBLIC_PREDICTION_FILE=public/latest_prediction.json

# Optional: Feature flags
NEXT_PUBLIC_SHOW_ADVANCED_METRICS=true
NEXT_PUBLIC_AUTO_REFRESH_SECONDS=300
